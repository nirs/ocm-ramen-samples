---
resources:
  - source.yaml
  - pvc.yaml
  - vm.yaml
  - service.yaml
labels:
  - pairs:
      appname: fedora
secretGenerator:
  - name: fedora
    files:
      - test_rsa.pub
generatorOptions:
  disableNameSuffixHash: true
